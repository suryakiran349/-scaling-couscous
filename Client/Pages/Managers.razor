@using MudBlazor
@using Shared.DTOs.CRM

@* <MudDataGrid Items="@ManagersList" Hover="true" Dense="true" Bordered="false" Striped="true">
    <Columns>
        <PropertyColumn Property="@(x => x.Name)" Title="Name" />
        <PropertyColumn Property="@(x => x.Email)" Title="E-Mail" />
        <PropertyColumn Property="@(x => x.Phone)" Title="Phone" />
        <PropertyColumn Property="@(x => x.Department)" Title="Department" />
        <PropertyColumn Property="@(x => x.IsActive)" Title="Active" />
    </Columns>
</MudDataGrid>

<MudText Class="mt-4" Color="Color.Primary" Style="cursor:pointer;">
    <MudIcon Icon="@Icons.Material.Filled.Add" /> Add New Manager
</MudText>

@code {
    [Parameter]
    public List<ManagerDto> ManagersList { get; set; } = new();
} *@
